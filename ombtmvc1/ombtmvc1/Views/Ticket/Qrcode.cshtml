@model ombtmvc1.Models.Ticket

@{ ViewBag.Title = "Qrcode"; }

<h2>Qrcode</h2>


@using (Html.BeginForm("Qrcode", "Ticket", FormMethod.Post))
{

<input type="submit" value="Generate" />}

<hr />
@if (ViewBag.QRCode != null)
{
<img src="@ViewBag.QRCode" style="width:200px;height:200px;" />}